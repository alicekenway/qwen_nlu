presently it takes the args directly, add one more mode, it can take args from yaml
just like the llamafactory

also, provide parameters, so I control which layers the lora apply to.
also I can decide use lora or not.
